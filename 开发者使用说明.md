# CalmPath 开发者使用说明

面向团队成员的本地运行手册(README.md 的中文版,内容对应)。

## 项目简介

CalmPath 是面向墨尔本 CBD 通勤者的感官友好步行路线对比 App。不是单纯找最快路线,而是基于行人拥挤度数据比较候选路线,推荐相对不那么拥挤的一条,并给出文字说明。

FIT5120 TA28 小组项目("Sensory-Friendly Urban Futures")。

## 当前实现范围

本版本已经把 Figma 原型的完整流程做出来了:

- **US 1.1** —— 每条路线显示感官等级(`Low Sensory` / `High Sensory` / `Sensory information unavailable`)
- **US 1.2** —— 识别并规避高拥挤路段,推荐相对更低拥挤度的路线
- **US 1.3**(仅原型级别)—— 选择的拥挤敏感度偏好(低/中/高)会在这次请求里调整分类阈值,并体现在推荐路线的文案上
- **US 2.1**(Stretch)—— 路线附近的"安静地点"候选推荐,含详情页和一个终态的"前往这个庇护点"确认交互

下一小时拥挤预测(US 2.2)明确**没有做**——本轮不做 ML/AI,见需求文档第4.2节。范围决策详见 [`CalmPath_App_Development_Requirements.md`](CalmPath_App_Development_Requirements.md) 第4节和第19-20节。

路由和地址搜索现在已经支持接入 **Google Maps Platform**(Directions API + Places API)作为真实数据源,做成了适配器模式(`RoutingProvider` / `PlacesProvider`)——在 `services/api/.env` 里填上 `GOOGLE_MAPS_API_KEY` 就能自动切换成真实数据,不用改任何代码。不填的话会自动退回演示路线生成器 + 小型地名对照表,所以现在没有 key 也能完整跑通。具体怎么申请和填,看下面"接入 Google Maps"这节。

City of Melbourne 开放数据接入目前还是占位/演示实现,等团队和导师确认后再接。安静地点数据也是种子数据:State Library Victoria 对应真实地点(标"verified"),另外两个(St Francis' Church Courtyard、Immigration Museum Courtyard)标为"prototype",在界面上会分别注明数据来源(对应产品原则14.2"保守描述,不能言过其实")。

## 技术栈

| 层 | 技术 |
|---|---|
| 移动端 | React Native(Expo,TypeScript)、Expo Router、TanStack Query、Zod、react-native-svg |
| 后端 API | FastAPI、SQLAlchemy 2.0、GeoAlchemy2、Alembic、Pydantic |
| 数据库 | PostgreSQL + PostGIS(用 Docker 跑) |

## 目录结构

```text
apps/
  mobile/              # Expo React Native App
services/
  api/                 # FastAPI 服务
infra/
  docker-compose.yml   # 本地 Postgres/PostGIS
```

相关文档:

- [`CalmPath_App_Development_Requirements.md`](CalmPath_App_Development_Requirements.md) —— 完整开发需求基线(需求、API 契约、数据库设计、验收标准、测试计划)
- [`CalmPath_Dev_Requirements_FE_BE.md`](CalmPath_Dev_Requirements_FE_BE.md) / [`_EN.md`](CalmPath_Dev_Requirements_FE_BE_EN.md) —— 精简版前后端开发需求(中/英)
- [`App使用说明.md`](App使用说明.md) —— App 功能使用手册(面向最终用户)

## 环境准备

- Python 3.11 及以上(本项目用 3.13 开发测试)
- Node.js 18 及以上 + npm
- Docker Desktop(跑 Postgres/PostGIS)

## 后端启动(`services/api`)

```bash
cd services/api
py -m venv .venv
./.venv/Scripts/pip install -r requirements-dev.txt   # Windows
# source .venv/bin/activate && pip install -r requirements-dev.txt   # macOS/Linux

cp .env.example .env
```

启动数据库:

```bash
cd ../../infra
docker compose up -d
```

跑数据库迁移 + 灌入演示数据:

```bash
cd ../services/api
./.venv/Scripts/python -m alembic upgrade head
./.venv/Scripts/python -m app.seed
```

启动 API:

```bash
./.venv/Scripts/python -m uvicorn app.main:app --reload --port 8000
```

> **Windows 注意事项**:8000 端口在部分 Windows 机器上会落在系统的 TCP 保留端口段里(用 `netsh interface ipv4 show excludedportrange protocol=tcp` 可以查),如果服务起不来,换个端口(比如 `--port 8010`),同时把 `apps/mobile/.env` 里的 `EXPO_PUBLIC_API_BASE_URL` 改成对应端口。

API 文档地址:`http://localhost:8000/docs`。健康检查:`GET /api/v1/health`。

### 后端测试

```bash
docker exec <postgres容器名> psql -U calmpath -d calmpath -c "CREATE DATABASE calmpath_test;"
./.venv/Scripts/python -m pytest
```

### 演示数据会过期

行人观测数据默认只在 30 分钟内有效(`max_observation_age_minutes` 配置项),超过之后所有路线都会变成"Sensory information unavailable"。如果隔了比较久要演示,重新灌一次数据:

```bash
docker exec <postgres容器名> psql -U calmpath -d calmpath -c "TRUNCATE pedestrian_observations, pedestrian_sensors CASCADE;"
./.venv/Scripts/python -m app.seed
```

## 接入 Google Maps(可选)

1. 去 [Google Cloud Console](https://console.cloud.google.com/) 建一个项目,开通 **Directions API** 和 **Places API** 这两个,然后建一个 API key。
2. 建议给 key 加限制:按"API限制"只允许这两个 API;暂时不用加 HTTP referrer 限制,因为这个 key 只在后端用,浏览器端永远看不到它。
3. 把 key 填进 `services/api/.env`:
   ```
   GOOGLE_MAPS_API_KEY=你的key
   ```
4. 重启后端进程(不要指望 `--reload` 能生效,见上面的提醒),之后 `/routes/compare` 会用真实步行路线,`/places/search` 会用真实地址搜索(仍然限制在墨尔本 CBD 附近,3公里搜索半径)。
5. 没填 key 的时候一切照常用演示数据,团队其他人不用等你申请到 key 才能跑项目。

> 地图**可视化**部分(路线详情页那个网格示意图)目前没有跟着换成真的 Google 地图瓦片——这是团队确认过的范围,只接真实路线数据和地址搜索,可视化还是保留 SVG 示意图,跟 Figma 原型的简化风格保持一致。

## 前端启动(`apps/mobile`)

```bash
cd apps/mobile
npm install
cp .env.example .env   # 把 EXPO_PUBLIC_API_BASE_URL 改成你本地跑起来的 API 地址
npm run web             # 或者: npm run android / npm run ios
```

App 打开后第一屏是 Preference Setup(选拥挤敏感度),然后才是 Destination。DestinationScreen 上有个 **"Use demo route"** 按钮,会自动填入种子数据对应的起终点(Flinders Street Station → State Library Victoria),不用自己找地名或坐标。在路线详情页点 **"Show quiet places"** 可以进入安静地点选择流程。

### 前端检查

```bash
npx tsc --noEmit
```

## 环境变量

| 文件 | 变量 | 作用 |
|---|---|---|
| `services/api/.env` | `DATABASE_URL` | Postgres 连接字符串 |
| `services/api/.env` | `CORS_ORIGINS` | 允许跨域访问的前端地址(开发环境) |
| `services/api/.env` | `CBD_MIN_LAT` / `CBD_MAX_LAT` / `CBD_MIN_LON` / `CBD_MAX_LON` | 墨尔本 CBD 服务边界的经纬度范围(占位值,等正式边界确认) |
| `services/api/.env` | `GOOGLE_MAPS_API_KEY` | 可选。填了就用真实路由+地址搜索,不填就用演示数据,见上一节 |
| `apps/mobile/.env` | `EXPO_PUBLIC_API_BASE_URL` | App 调用后端的地址,比如 `http://localhost:8000/api/v1` |

Google Maps 的 key 只在后端用(前端从来不会拿到这个 key,请求都是先打到我们自己的后端,再由后端转发给 Google)。

## 已知局限

- 没配 `GOOGLE_MAPS_API_KEY` 的时候,路由用固定的两条演示路线,地址搜索用5个地名的小型对照表。
- 地图**可视化**还是 SVG 示意图,没有换成真实地图瓦片(团队确认过的范围,只接数据不接可视化)。
- 行人数据是手动灌的种子数据,不是从 City of Melbourne 开放数据接入的(而且30分钟就会过期,见上文)。
- 安静地点数据也是手动灌进 `places` 表的种子数据,不是真实的开放数据搜索。
- 没有账号系统、离线地图、逐步导航、下一小时预测(本轮范围外,见需求文档第4.4节)。
- "Walk to this refuge" 只是界面上的终态确认,不是真实的步行导航。

后端 `--reload` 在 Windows 上偶尔会漏掉连续快速的文件改动(WatchFiles 的问题),如果发现改了代码但接口行为没变,别指望 reload,直接手动重启一遍后端进程。

还有哪些悬而未决的问题,看需求文档第20节"Decisions Required Before Development"。
